# Server
PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sehat_awaaz
DB_USER=sehat_admin
DB_PASSWORD=sehat_dev_password

# JWT
JWT_SECRET=sehat-awaaz-dev-secret-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=sehat-awaaz-refresh-dev-secret-change-in-production
JWT_REFRESH_EXPIRES_IN=7d

# NLU Service (Python FastAPI)
NLU_SERVICE_URL=http://localhost:8001

# OTP (dev mode - prints to console)
OTP_MODE=mock
OTP_EXPIRY_SECONDS=300
